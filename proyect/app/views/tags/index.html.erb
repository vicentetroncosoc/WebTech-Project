<h1 class="mb-3">Tags</h1>
<ul class="list-group">
  <% @tags.each do |t| %>
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <%= link_to t.name, t %>
      <span class="badge text-bg-secondary"><%= t.challenges.count %></span>
    </li>
  <% end %>
</ul>
